import{Renderer}from"./game/Renderer.js";import{AudioManager}from"./game/AudioManager.js";import{Game}from"./game/Game.js";import{InputManager}from"./game/InputManager.js";import{LeaderboardService}from"./services/LeaderboardService.js";import{UIController}from"./ui/UIController.js";import{STORAGE_PREFIX}from"./config.js";
const audio=new AudioManager,service=new LeaderboardService,renderer=new Renderer(document.getElementById("game"));let oldBest=0;const game=new Game(renderer,audio,run=>ui.over(run,oldBest));const ui=new UIController(service,audio,username=>{oldBest=Number(localStorage.getItem(STORAGE_PREFIX+"best:"+username)||0);game.start(username)});new InputManager(window,type=>{if(game.state==="GAME_OVER"&&type==="press")ui.start(true);else game.input(type)});document.addEventListener("visibilitychange",()=>document.hidden?game.pause():game.resume());setInterval(()=>{if(game.state==="PLAYING")ui.hud(game.score(),oldBest,game.combo)},80);service.retry();
addEventListener("keydown",event=>{if(["Space","ArrowUp","KeyW"].includes(event.code)&&game.state==="MENU"&&document.activeElement!==document.getElementById("username")){event.preventDefault();ui.start()}});
