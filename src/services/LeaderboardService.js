import{LEADERBOARD_API_URL,STORAGE_PREFIX,GAME_VERSION}from"../config.js";
export function dedupeTop(scores,limit=10){const best=new Map;for(const s of scores||[]){const username=String(s.username||"").trim(),score=Number(s.score);if(/^[A-Za-z0-9_-]{3,16}$/.test(username)&&Number.isFinite(score)&&score>=0)best.set(username,Math.max(best.get(username)||0,Math.floor(score)))}return[...best].map(([username,score])=>({username,score})).sort((a,b)=>b.score-a.score||a.username.localeCompare(b.username)).slice(0,limit)}
export function validSubmission(s){return/^[A-Za-z0-9_-]{3,16}$/.test(s.username)&&["score","distance","durationMs","maxCombo","targetsDestroyed"].every(k=>Number.isInteger(s[k])&&s[k]>=0)&&s.durationMs<=86400000&&s.score<=Math.max(5000,s.durationMs*2)}
export class LeaderboardService{constructor(){this.api=LEADERBOARD_API_URL;this.localKey=STORAGE_PREFIX+"scores";this.cacheKey=STORAGE_PREFIX+"cache";this.queueKey=STORAGE_PREFIX+"queue";this.sent=new Set}read(k,f=[]){try{return JSON.parse(localStorage.getItem(k))||f}catch{return f}}local(){return dedupeTop(this.read(this.localKey))}async top(){if(!this.api)return{scores:this.local(),mode:"LOCAL"};try{const ctl=new AbortController,t=setTimeout(()=>ctl.abort(),4000),r=await fetch(`${this.api}?action=top&limit=10`,{signal:ctl.signal});clearTimeout(t);const j=await r.json();if(!r.ok||!j.ok)throw Error();const scores=dedupeTop(j.scores);localStorage.setItem(this.cacheKey,JSON.stringify(scores));return{scores,mode:"GLOBAL"}}catch{return{scores:dedupeTop(this.read(this.cacheKey,this.local())),mode:"OFFLINE"}}}async submit(run){const entry={...run,gameVersion:GAME_VERSION},id=`${entry.username}:${entry.score}:${entry.durationMs}:${entry.seed}`;if(this.sent.has(id))return;this.sent.add(id);const local=dedupeTop([...this.read(this.localKey),entry],100);localStorage.setItem(this.localKey,JSON.stringify(local));if(!this.api)return;try{const body=new URLSearchParams(Object.entries(entry).filter(([k])=>k!=="seed"));const ctl=new AbortController,t=setTimeout(()=>ctl.abort(),4500),r=await fetch(this.api,{method:"POST",body,signal:ctl.signal});clearTimeout(t);const j=await r.json();if(!r.ok||!j.ok)throw Error()}catch{const q=this.read(this.queueKey);if(!q.some(x=>x.id===id))q.push({id,entry});localStorage.setItem(this.queueKey,JSON.stringify(q.slice(-20)))}}async retry(){const q=this.read(this.queueKey);localStorage.removeItem(this.queueKey);for(const x of q){this.sent.delete(x.id);await this.submit(x.entry)}}}
